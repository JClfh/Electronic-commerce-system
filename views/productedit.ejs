<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>product</title>
        <link rel="stylesheet" href="/css/public/header.css">
        <link rel="stylesheet" href="/css/public/prolist.css">
        <link rel="stylesheet" href="/css/productadd.css">
        <link rel="stylesheet" href="/bootstrap-4.3.1-dist/css/bootstrap.min.css">
        <script src="/js/jquery-3.3.1.min.js"></script>
        <script src="/bootstrap-4.3.1-dist/js/bootstrap.min.js"></script>
    </head>
    <body>
        <%- include('header.ejs')  -%>
        <div class="main-content">
            <%- include('prolist.ejs')  -%>
            <div class="product-page">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="#">商品管理</a></li>
                        <li class="breadcrumb-item active" aria-current="page">编辑商品</li>
                    </ol>
                </nav>
                <div class="card mb-3" style="width: 100%;">
                    <div class="card-header">修改商品</div>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">
                            <form role="form"  method="post" enctype="multipart/form-data">
                                
                                <div class="form-group row">
                                    <label for="number" class="col-1 col-form-label">商品类别</label>
                                    
                                    <select class="form-control col-6"  name="category">
                                     <% for(var i = 0 ; i < category.length;i++){%>
                                        <% if(products.category._id.toString() == category[i]._id.toString()){%>
                                            
                                            <option value="<%=category[i]._id%>" selected><%=category[i].name%></option>
                                            <% } else { %>
                                            <option value="<%=category[i]._id%>"><%=category[i].name%></option>
                                            <%}%>
                                     <%}%>

                                   </select>
                                </div>
                                <div class="form-group row">
                                    <label for="proName" class="col-1 col-form-label">商品名称</label>
                                    <input type="text" class="form-control col-6" id="proName" name="proName" value="<%= products.proName %>"required>
                                </div>
                                <div class="form-group row">
                                    <label for="proImg" class="col-1 col-form-label">商品图片</label>
                                    <input type="file" id="proImg" class="col-6 form-control-plaintext" name="proImg" value="<%= products.proImg %>"><br>
                                </div>
                                <div class="form-group row">
                                    <label for="preview" class="col-1 col-form-label">图片预览</label>
                                    <img src="<%= products.proImg %>" id="preview" width="200" height="200" alt="/<%= products.proName %>">
                                </div>
                                <div class="form-group row">
                                    <label for="procost" class="col-1 col-form-label">商品价格</label>
                                    <input type="text" id="procost" class="form-control col-6" name="procost" value="<%= products.procost %>"required>
                                </div>
                                <div class="form-group row">
                                    <label for="inventory" class="col-1 col-form-label">库存</label>
                                    <input type="text" id="inventory" class="form-control col-6" name="inventory" value="<%= products.inventory %>">
                                </div>
                                <div class="form-group row">
                                    <label for="description" class="col-1 col-form-label">商品描述</label>
                                    <!-- 注意：input是单标签，有value值可以设置，但是 textarea是双标签，没有value值，直接在内部输出 -->
                                    <textarea id="description" class="form-control col-6" rows="3" name="description"><%= products.description %></textarea>
                                </div>
                                <input type="submit" class="btn btn-primary" value="提交">
                            </form>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </body>
</html>
